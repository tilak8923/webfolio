// Firebase is completely disabled in this project.
// Storing configurations and database records statically in portfolio-data.ts.

export const isFirebaseConfigured = false;
export const db = null;
export const auth = null;
export const googleProvider = null;
